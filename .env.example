# PodcastIndex API (https://api.podcastindex.org) — register a free account for these.
PODCASTINDEX_API_KEY=
PODCASTINDEX_API_SECRET=

# RunPod API (https://www.runpod.io) — used for pod create/list/stop/terminate only;
# RunPod exposes no log-retrieval API, see infra/bootstrap.sh + pipeline/heartbeat.py.
RUNPOD_API_KEY=

# Cloudflare R2 (S3-compatible object storage)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
# Namespaces every clips/manifest/status/db_snapshots/logs key under this
# prefix (defaults to "v2" if unset, see config.EnvSecrets.r2_key_prefix) --
# an earlier batch already left clips/status objects in some buckets at the
# bare, unprefixed paths with no surviving db_snapshot/manifest to back them.
# Bump this for any new run that should not mix with what's already there.
R2_KEY_PREFIX=v2

# Hugging Face token with access to pyannote/speaker-diarization-3.1 and the
# embedding model's gated-model agreements accepted on huggingface.co.
HF_TOKEN=

# Budget / time guardrails (see pipeline/costs.py)
BUDGET_CAP_USD=100
TIME_CAP_HOURS=24
